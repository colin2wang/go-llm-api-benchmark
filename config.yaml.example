# config.yaml.example -- LLM API Benchmark main config template
# Copy to config.yaml, set the provider type and the path to its dedicated config file.
provider: huzhouai
config_file: config.huzhouai.yaml
